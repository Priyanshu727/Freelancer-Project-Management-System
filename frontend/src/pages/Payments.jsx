import React from 'react';
import PaymentList from '../components/PaymentList';

const Payments = () => {
    return (
        <div>
            <PaymentList />
        </div>
    );
};

export default Payments;
